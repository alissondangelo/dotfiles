# ~/.config/starship.toml

# Don't print a new line at the start of the prompt
add_newline = false

# Use custom format
format = """
$directory$rust$package$status
$character"""

# Disable the package module, hiding it from the prompt completely
[package]
disabled = true