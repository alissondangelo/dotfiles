#!/usr/bin/env bash
# Toggle picom

if pgrep picom; then
    pkill picom &
    notify-send "🎮" -r 1 --urgency low
else
    picom --config  $HOME/.config/picom/picom.conf &
    notify-send "🕶" -r 1 --urgency low
fi
